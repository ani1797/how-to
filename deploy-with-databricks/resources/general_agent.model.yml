# yaml-language-server: $schema=../.schema/databricks.json
resources:
  models:
    general_agent:
      name: ${var.general_agent_model_name}
      description: |
        General Agent Model

targets:
  dev:
    variables:
      general_agent_model_name: general_agent
  staging:
    variables:
      general_agent_model_name: general_agent_staging
  prod:
    variables:
      general_agent_model_name: general_agent_prod
